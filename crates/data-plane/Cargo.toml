[package]
name = "data-plane"
version = "0.1.0"
edition = "2024"

[features]
enclave = ["shared/enclave"]

[dependencies]
shared = { path = "../shared" }
tokio = { version = "1", features = ["full"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
libc = "0.2"
