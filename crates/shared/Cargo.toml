[package]
name = "shared"
version = "0.1.0"
edition = "2024"

[features]
enclave = ["dep:tokio-vsock"]

[dependencies]
tokio = { version = "1", features = ["full"] }
async-trait = "0.1"
tokio-vsock = { version = "0.5", optional = true }
