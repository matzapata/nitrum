{
  "name": "blockchain-wallet-client",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "CLI client for the blockchain-wallet enclave sample",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "wallet:create": "npm run build && node dist/index.js wallet create",
    "wallet:sign": "npm run build && node dist/index.js sign"
  },
  "dependencies": {
    "ethers": "6.13.0",
    "nitrum-node": "0.0.1",
    "axios": "1",
    "https": "1"
  },
  "devDependencies": {
    "@types/node": "25.5.2",
    "typescript": "6.0.2"
  },
  "engines": {
    "node": ">=18"
  }
}

