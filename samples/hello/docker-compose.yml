# Local development compose for the enclaves.
#
# Usage:
#   docker compose up            # start (previously built hello image)
#   docker compose down          # stop
#
# For domain nitrum.local (e.g. ACME cert), add to your host /etc/hosts:
#   127.0.0.1 nitrum.local
# Then you can use: curl -sk https://nitrum.local/...
#
# Test the enclave well-known endpoints:
#   curl -sk https://nitrum.local/.well-known/enclave/status
#   curl -sk https://nitrum.local/.well-known/enclave/attestation
#
# Test the user app through TLS ingress:
#   curl -sk https://nitrum.local/health
#
# Access the internal data-plane API directly (no TLS):
#   curl http://nitrum.local/attestation -d '{}'
#
# Test crypto (encrypt/decrypt via app; plaintext is UTF-8 string):
#   curl -sk https://nitrum.local/crypto -H "Content-Type: application/json" -d '{"plaintext":"hello"}'

configs:
  pebble-config:
    content: |
      {
        "pebble": {
          "listenAddress": "0.0.0.0:14000",
          "managementListenAddress": "0.0.0.0:15000",
          "certificate": "test/certs/localhost/cert.pem",
          "privateKey": "test/certs/localhost/key.pem",
          "httpPort": 5002,
          "tlsPort": 5001,
          "ocspResponderURL": "",
          "externalAccountBindingRequired": false,
          "retryAfter": { "authz": 1, "order": 1 },
          "profiles": {
            "default": {
              "description": "Short-lived certs for testing renewal",
              "validityPeriod": 10
            }
          }
        }
      }

  aemm-config:
    content: |
      {
        "imdsv2": true,
        "server": {
          "hostname": "0.0.0.0",
          "port": "1338"
        },
        "metadata": {
          "paths": {
            "iam-security-credentials": "/latest/meta-data/iam/security-credentials/nitrum-local-role"
          },
          "values": {
            "iam-info": {
              "Code": "Success",
              "LastUpdated": "2024-01-01T00:00:00Z",
              "InstanceProfileArn": "arn:aws:iam::000000000000:instance-profile/nitrum-local-role",
              "InstanceProfileId": "AIPANITRUMLOCAL00"
            },
            "iam-security-credentials-role": "nitrum-local-role",
            "iam-security-credentials": {
              "Code": "Success",
              "LastUpdated": "2024-01-01T00:00:00Z",
              "Type": "AWS-HMAC",
              "AccessKeyId": "test",
              "SecretAccessKey": "test",
              "Token": "",
              "Expiration": "2099-01-01T00:00:00Z"
            }
          }
        }
      }

services:
  # Amazon EC2 Metadata Mock (IMDSv2 + fake instance profile credentials).
  imds:
    image: public.ecr.aws/aws-ec2/amazon-ec2-metadata-mock:v1.13.0
    platform: linux/amd64
    configs:
      - source: aemm-config
        target: /config/aemm-config.json
    command: ["-c", "/config/aemm-config.json"]
    ports:
      - "1338:1338"

  # LocalStack: SSM + DynamoDB + KMS on 4566 (data-plane SSM, table, symmetric DEK).
  localstack:
    image: localstack/localstack:latest
    environment:
      - SERVICES=ssm,dynamodb,kms
      - AWS_DEFAULT_REGION=us-east-1
    ports:
      - "4566:4566"

  # One-shot: create DynamoDB table + SSM params expected by the data-plane (see crates/data-plane/src/utils/ssm.rs).
  localstack-init:
    image: amazon/aws-cli:latest
    depends_on:
      - localstack
    environment:
      AWS_ACCESS_KEY_ID: test
      AWS_SECRET_ACCESS_KEY: test
      AWS_DEFAULT_REGION: us-east-1
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        set -e
        echo "Waiting for LocalStack..."
        for i in $$(seq 1 60); do
          if aws --endpoint-url=http://localstack:4566 dynamodb list-tables >/dev/null 2>&1; then
            break
          fi
          sleep 1
        done
        
        aws --endpoint-url=http://localstack:4566 dynamodb create-table \
          --table-name nitrum-dev \
          --attribute-definitions AttributeName=pk,AttributeType=S \
          --key-schema AttributeName=pk,KeyType=HASH \
          --billing-mode PAY_PER_REQUEST 2>/dev/null || true
        
        KMS_KEY_ID=$$(aws --endpoint-url=http://localstack:4566 kms create-key \
          --description nitrum-dev-symmetric-data-key \
          --query KeyMetadata.KeyId --output text)
        
        aws --endpoint-url=http://localstack:4566 ssm put-parameter \
          --name /nitrum/kms_key_id --value "$$KMS_KEY_ID" --type String --overwrite
        
        aws --endpoint-url=http://localstack:4566 ssm put-parameter \
          --name /nitrum/dynamodb_table --value nitrum-dev --type String --overwrite
        
        echo "LocalStack init done."

  pebble:
    image: ghcr.io/letsencrypt/pebble:latest
    command: ["-config", "/config/pebble-config.json"]
    ports:
      - "14000:14000"
      - "15000:15000"
    configs:
      - source: pebble-config
        target: /config/pebble-config.json
    environment:
      PEBBLE_VA_NOSLEEP: "1"

  enclave:
    image: ${ENCLAVE_IMAGE}
    pull_policy: never
    hostname: nitrum.local
    ports:
      - "443:443"
      - "5002:5002"
    networks:
      default:
        aliases:
          - nitrum.local
    depends_on:
      localstack-init:
        condition: service_completed_successfully
      imds:
        condition: service_started
      pebble:
        condition: service_started
    environment:
      RUST_LOG: info

      # Only for local dev
      NITRUM_IMDS_BASE_URL: http://imds:1338/latest
      NITRUM_SSM_ENDPOINT_URL: http://localstack:4566
      NITRUM_DYNAMODB_ENDPOINT_URL: http://localstack:4566
      NITRUM_ACME_HTTP01_LISTEN_ADDR: "0.0.0.0:5002"
      NITRUM_ACME_DIRECTORY_URL: https://pebble:14000/dir
      NITRUM_KMS_ENDPOINT_URL: http://localstack:4566
      PEBBLE_MINICA_CERT: |
        -----BEGIN CERTIFICATE-----
        
        -----END CERTIFICATE-----
